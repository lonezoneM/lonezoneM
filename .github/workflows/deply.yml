name: Metrics
on:
  # Schedule daily updates
  schedule: [{cron: "0 0 * * *"}]
  # (optional) Run workflow manually
  workflow_dispatch:
  # (optional) Run workflow when pushing on master/main
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest #  FIXED: Pointing to the official compiled action engine
        with:
          # Your GitHub token
          token: ${{ secrets.METRICS_TOKEN }}

          # Options (Your personal configuration remains perfectly safe!)
          user: lonezoneM
          template: classic
          base: header, activity, community, repositories, metadata
          config_timezone: Asia/Dhaka
          plugin_introduction: yes
          plugin_introduction_title: yes
          plugin_stargazers: yes
          plugin_stargazers_charts_type: classic
          plugin_fortune: yes
          plugin_languages: yes
          plugin_languages_analysis_timeout: 15
          plugin_languages_categories: markup, programming
          plugin_languages_colors: github
          plugin_languages_limit: 8
          plugin_languages_recent_categories: markup, programming
          plugin_languages_recent_days: 14
          plugin_languages_recent_load: 300
          plugin_languages_sections: most-used
          plugin_languages_threshold: 0%
          plugin_screenshot: yes
          plugin_screenshot_title: An random image -
          plugin_screenshot_url: https://c.xkcd.com/random/comic/
          plugin_screenshot_selector: "#comic"
          
